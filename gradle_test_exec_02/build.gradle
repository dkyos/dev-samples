
task hello1 << {
	new ByteArrayOutputStream().withStream { os ->
		def result = exec {
			executable = 'ls'
			args = ['-al']
			standardOutput = os
		}
		def outputAsString = os.toString()
		println "Output: $outputAsString ";
	}
}


task hello2(type: Exec) {
	executable "sh"
	args "-c", "echo 'hello from your shell'"
}



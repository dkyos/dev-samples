
task hello3{
    def stdout = new ByteArrayOutputStream()
    exec {
        ignoreExitValue = true
        executable "/home/dkyun77/tizen-sdk/tools/sdb"
        args "help"
        standardOutput = stdout;
    }
}

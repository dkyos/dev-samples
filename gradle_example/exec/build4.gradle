
def PWD='/home/dkyun77/tizen-sdk/tools/'

task hello(type:Exec) {
    workingDir PWD

    ignoreExitValue = true

    //commandLine 'cmd', '/c', 'stop.bat'
    commandLine './sdb', 'help'

    standardOutput = new ByteArrayOutputStream()

    ext.output = {
        return standardOutput.toString()
    }
}

